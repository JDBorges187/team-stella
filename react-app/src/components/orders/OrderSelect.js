import React from 'react';

const OrderSelect = ({orders, setOrderId}) => {

    return (
        <div>
            <form>
                <select>

                </select>
            </form>
        </div>
    )
}

export default OrderSelect;
